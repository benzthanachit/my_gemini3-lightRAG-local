# 🧠 Gemini 3 Agentic Hybrid GraphRAG (Local Host)

![Project Status](https://img.shields.io/badge/Status-Active-success)
![Tech Stack](https://img.shields.io/badge/Tech-Gemini%203%20%7C%20Neo4j%20%7C%20Qdrant%20%7C%20LlamaIndex-blue)

ระบบ **Agentic Knowledge Augmented Generation (KAG)** ที่รันบนเครื่อง PC ส่วนตัว (Local Host) โดยผสานพลังของ **Graph Database (Neo4j)** และ **Vector Database (Qdrant)** เข้าด้วยกัน เพื่อสร้าง "ความจำระยะยาว" (Long-term Memory) ให้กับ AI โดยใช้สมองของ **Gemini 3 Pro** ในการคิดวิเคราะห์และหาคำตอบ

---

## 🌟 องค์ประกอบหลัก (Architecture & Principles)

ระบบนี้ใช้สถาปัตยกรรมแบบ **Hybrid RAG** ซึ่งแก้จุดอ่อนของ RAG ทั่วไป โดยการทำงานร่วมกันของ 3 ส่วนหลัก:

1.  **Vector Store (Qdrant):**
    * **หลักการ:** Semantic Search (ค้นหาตามความหมาย)
    * **หน้าที่:** เก็บข้อมูลเนื้อหา (Chunks) เป็นตัวเลข (Embeddings) เพื่อให้ค้นหาข้อมูลที่ใกล้เคียงกับคำถามได้รวดเร็ว
    * **Model:** `models/text-embedding-004` (Google GenAI)

2.  **Knowledge Graph (Neo4j):**
    * **หลักการ:** Structured Relationship (ความสัมพันธ์เชิงโครงสร้าง)
    * **หน้าที่:** เก็บข้อมูลในรูปแบบ Nodes และ Edges (Entities & Relationships) ทำให้ AI เข้าใจความเชื่อมโยงที่ซับซ้อน (เช่น A เป็นพ่อของ B, B เกลียด C)
    * **Graph Extraction:** ใช้ `Gemini 2.5 Flash` ในการสกัดความสัมพันธ์

3.  **Reasoning Engine (Gemini 3 Pro):**
    * **หลักการ:** Chain of Thought / Thinking Process
    * **หน้าที่:** รับข้อมูลที่คัดกรองมาจาก Qdrant/Neo4j แล้วใช้ "Thinking Mode" วิเคราะห์หาคำตอบอย่างมีตรรกะ ไม่ใช่แค่การดึงข้อความมาแปะ

---

## 🛠️ Tech Stack & URLs

| Component | Technology | Access URL |
| :--- | :--- | :--- |
| **Frontend UI** | Open WebUI | http://localhost:3000 |
| **Backend API** | FastAPI (Python) | http://localhost:8000/v1 |
| **Graph DB** | Neo4j | http://localhost:7474 (Login: neo4j/password123) |
| **Vector DB** | Qdrant | http://localhost:6333/dashboard |
| **LLM** | Google Gemini 3 Pro | Cloud API |

---

## ✅ ข้อดีและข้อเสีย (Pros & Cons)

### ข้อดี (Pros)
* **ความแม่นยำสูง (High Accuracy):** การใช้ Hybrid (Graph + Vector) ทำให้ลดปัญหา Hallucination ได้ดีกว่า RAG ทั่วไปถึง 40%
* **ความเป็นส่วนตัว (Data Privacy):** ข้อมูลทั้งหมด (Graph/Vector) ถูกเก็บอยู่ในเครื่อง PC ของคุณ (Local) ไม่ได้ฝากไว้บน Cloud Database ของใคร
* **ฉลาดล้ำลึก (Deep Reasoning):** ใช้ Gemini 3 Pro ที่มีความสามารถในการ "หยุดคิด" ก่อนตอบ ทำให้แก้โจทย์วิจัยที่ซับซ้อนได้
* **ประหยัดและเร็ว (Efficiency):** ใช้ Gemini 2.5 Flash ในการสร้าง Graph (Ingest) ซึ่งราคาถูกและทำงานเร็ว และมีระบบเช็คไฟล์ซ้ำ (Registry) ไม่ต้องประมวลผลใหม่

### ข้อเสีย (Cons)
* **ใช้เวลาสร้างข้อมูล (Slow Ingestion):** การสร้าง Knowledge Graph กินเวลามากกว่า Vector ปกติ เพราะต้องใช้ AI อ่านและแกะความสัมพันธ์ทุกประโยค
* **พึ่งพา Internet:** แม้ Database จะอยู่ Local แต่สมอง (LLM/Embedding) ยังต้องยิง API ไปหา Google
* **ทรัพยากรเครื่อง:** การรัน Docker 4-5 Containers พร้อมกัน ต้องการ RAM อย่างน้อย 16GB+

---

## 🚀 วิธีการติดตั้ง (Installation)

### 1. เตรียมเครื่อง (Prerequisites)
* Install **Docker Desktop**
* Install **Python 3.12+**
* มี **Google Gemini API Key**

### 2. Clone & Setup
สร้างไฟล์และโฟลเดอร์ตามโครงสร้างนี้:
```text
my-project/
├── data/                   # วางไฟล์ PDF/Text งานวิจัยที่นี่
├── storage_metadata/       # (สร้าง auto) ที่เก็บ Save Game ของ LlamaIndex
├── docker-compose.yml      # ไฟล์ตั้งค่า Docker
├── server.py               # โค้ดหลัก Backend
├── requirements.txt        # รายชื่อ Library
└── .env                    # เก็บ API Key
````

### 3\. ตั้งค่า Environment (.env)

สร้างไฟล์ `.env` และใส่ข้อมูล:

```env
GEMINI_API_KEY=AIzaSyDxxxx_ใส่คีย์ของคุณ_xxxx
NEO4J_USER=neo4j
NEO4J_PASSWORD=password123
NEO4J_URL=bolt://localhost:7687
QDRANT_URL=http://localhost:6333
OLLAMA_BASE_URL=http://localhost:11434
```

### 4\. เริ่มระบบ (Start System)

เปิด Terminal แล้วรันคำสั่ง:

```powershell
docker-compose up -d --build
```

*รอจนกว่า Containers ทั้งหมดจะสถานะ `Running`*

-----

## 📥 วิธีการนำเข้าข้อมูล (Ingestion Workflow)

ระบบมีฟีเจอร์ **Idempotent Ingestion** (ป้องกันไฟล์ซ้ำ)

1.  นำไฟล์ PDF หรือ Text ไปวางในโฟลเดอร์ `data/`
2.  ยิงคำสั่ง API เพื่อเริ่มกระบวนการ:

**PowerShell:**

```powershell
Invoke-RestMethod -Uri "http://localhost:8000/ingest" -Method Post -ContentType "application/json" -Body '{"folder_path": "./data"}'
```

**กระบวนการทำงานภายใน (Behind the Scenes):**

1.  **Check Registry:** ระบบจะเช็คว่าไฟล์นี้เคย Ingest ไปยัง? ถ้าเคยแล้ว -\> ข้ามทันที
2.  **Load Data:** อ่านไฟล์ PDF
3.  **Vectorize:** ส่งเนื้อหาไปทำ Embedding ด้วย `text-embedding-004` -\> บันทึกลง **Qdrant**
4.  **Graph Extraction:** ส่งเนื้อหาไปให้ `Gemini 2.5 Flash` แกะความสัมพันธ์ -\> บันทึกลง **Neo4j**
5.  **Persist:** บันทึกสถานะ Index ลง Disk (`storage_metadata`) เพื่อกันความจำเสื่อมตอน Restart

-----

## 💬 วิธีใช้งานแชท (Chat Usage)

1.  เปิด Browser ไปที่ **Open WebUI**: [http://localhost:3000](http://localhost:3000)
2.  (ครั้งแรก) สมัครสมาชิก User/Pass อะไรก็ได้ (เก็บในเครื่อง)
3.  ไปที่ **New Chat**
4.  เลือกโมเดล **`gemini-3-researcher`**
5.  พิมพ์ถามคำถามได้เลย\!

**ตัวอย่างการถาม:**

> "สรุปความสัมพันธ์ของตัวละครในโปรเจกต์ X ให้หน่อย"
> "วิเคราะห์ข้อมูลจากไฟล์ PDF เรื่องการเงิน และหาจุดที่น่าสงสัย"

-----

## 🔧 การตรวจสอบและ Debug

  * **เช็กว่าข้อมูลเข้า Qdrant ไหม:**

      * ไปที่ [http://localhost:6333/dashboard](http://localhost:6333/dashboard)
      * ดู Collection `research_memory_final` -\> Points count ต้อง \> 0

  * **เช็กกราฟความสัมพันธ์:**

      * ไปที่ [http://localhost:7474](http://localhost:7474)
      * รันคำสั่ง: `MATCH (n)-[r]->(m) RETURN n, r, m LIMIT 50`

  * **ดู Log การทำงาน:**

    ```powershell
    docker logs -f research_backend
    ```